---
'@ankhorage/runtime': minor
---

Resolve canonical `{ mediaId }` component-property references through the app media registry. External URLs resolve immediately, while storage and bundled assets use a provider-neutral host resolver with cached async resolution and safe unresolved fallbacks.
