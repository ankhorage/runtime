---
'@ankhorage/runtime': minor
---

Resolve canonical `{ mediaId }` component-property references through the app media registry, with immediate external URL handling and a provider-neutral host resolver for storage and bundled media.
